program Sample;
var x: integer;
    pi: real;
    msg: string;
begin
    x := 10;
    pi := 3.14159;
    msg := 'Hello, World!';
    // This is a comment
    { Multi-line
      comment }
    if x > 5 then
        x := x + 1;
    (* Another block comment *)
    writeln(msg);
end.
